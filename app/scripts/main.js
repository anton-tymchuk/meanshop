$(document).ready(function () {
    'use strict';

    $('.header--fixed').find('.header--hidden').css({ display: 'none' });
    // $('.header--fixed').find('.header--hidden').addClass('hidden');
});
